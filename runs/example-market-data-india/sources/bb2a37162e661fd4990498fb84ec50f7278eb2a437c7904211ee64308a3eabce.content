A local-first financial data warehouse for universe-scale market data.
Market Data Warehouse is designed for storing and analyzing historical OHLCV data across equities, futures, and volatility indices, with a clear path from daily bars → intraday data → production analytics.
- Parquet data lake → canonical storage
- DuckDB → local analytics, research, backtesting
- ClickHouse (optional) → large-scale aggregation & concurrency
- 
Daily ingestion for: - Equities (IB)
- Futures (IB)
- Volatility indices (CBOE API)
 
- 
Per-ticker bronze Parquet snapshots 
- 
Atomic writes + validation 
- 
Fallback recovery pipeline for missing data 
- 
On-demand DuckDB rebuilds from Parquet 
In one sentence: A local-first, production-ready market data warehouse for serious quantitative workflows.
- ⚡ High-performance local quant research environment
- 🧱 Scalable multi-asset data model
- 🔁 Clean local → production transition
- 🌐 Polyglot workflows (Python, Rust, Node.js)
Raw → Bronze → Silver → Gold
- Raw → vendor data
- Bronze → canonical Parquet (primary ingestion layer)
- Silver → cleaned / adjusted datasets
- Gold → analytics, factors, derived tables
- System of record: Parquet (data-lake/)
- Local engine: DuckDB
- Warehouse (optional): ClickHouse
~/market-warehouse/
├── data-lake/
│   ├── raw/
│   ├── bronze/
│   │   ├── asset_class=equity/symbol=AAPL/data.parquet
│   │   ├── asset_class=volatility/symbol=VIX/data.parquet
│   │   └── asset_class=futures/symbol=ES_202506/data.parquet
│   ├── silver/
│   └── gold/
├── duckdb/
│   └── market.duckdb
├── scripts/
├── logs/
└── .venv/
- macOS (Apple Silicon recommended)
- Homebrew
- Python 3.12+
- Node.js 22+
- DuckDB
- Interactive Brokers account
- Docker (recommended for IB Gateway)
- ClickHouse (optional)
chmod +x setup_market_warehouse.sh
./setup_market_warehouse.sh./setup_market_warehouse.sh \
  --start-clickhouse \
  --init-clickhouse \
  --with-sample-data \
  --smoke-testInteractive Brokers Gateway
You need a running IB Gateway for ingestion.
cd docker/ib-gateway
cp .env.example .env
mkdir -p secrets
echo "YOUR_IB_PASSWORD" > secrets/ib_password.txt
docker compose up -d- 
Complete 2FA via: - VNC (localhost:5900)
- IBKR mobile app
 
- VNC (
| Host | Purpose | 
|---|---|
| 4001 | Live API | 
| 4002 | Paper API | 
| 5900 | VNC | 
Run the same Docker Compose setup on a cloud VPS with Tailscale for secure, WireGuard-encrypted access. No public ports exposed — all traffic flows through a private mesh VPN.
- IB Gateway runs 24/5 without tying up a local machine
- Accessible from any Tailscale-enrolled device (Mac, iPhone, other servers)
- ~$4-6/mo (Hetzner) + free (Tailscale)
Hetzner VPS (all public ports blocked)
├── Tailscale (MagicDNS hostname: ib-gateway)
├── socat proxy: tailscale-ip:4001 → localhost:4001
└── Docker: same docker-compose.yml, unmodified
        └── IB Gateway (127.0.0.1:4001)
Clients connect to ib-gateway:4001 via Tailscale tunnel
- Provision a VPS (Hetzner CPX11 or similar, Ubuntu 24.04, US East region)
- Harden the host: non-root user, SSH key-only, disable root login, unattended-upgrades
- Install Docker and Tailscale on the VPS
- Configure Tailscale:
- Authenticate with a preauth key tagged for the gateway
- Enable MagicDNS for stable hostname resolution
- Set up ACLs to restrict which devices can reach port 4001
 
- Bridge Tailscale to Docker: a socat systemd service forwards traffic from the Tailscale interface to Docker's localhost-bound ports (tailscale serve --tcpadds TLS, which is incompatible with IB's raw TCP protocol)
- Firewall: ufw denies all incoming except on the Tailscale interface
- Deploy: scpthedocker-compose.ymland.envto the VPS, create the password secret,docker compose up -d
- 2FA: Approve via IBKR mobile app or VNC through Tailscale
# Set in .env or export in shell
MDW_IB_HOST=ib-gateway    # Tailscale MagicDNS hostname
MDW_IB_PORT=4001All scripts (fetch_ib_historical.py, daily_update.py, etc.) read MDW_IB_HOST/MDW_IB_PORT automatically.
SSH from iOS/Android (Termius, Blink) to mdw@ib-gateway via Tailscale:
cd ~/ib-gateway
docker compose stop    # stop gateway
docker compose start   # start gateway
docker compose ps      # check statusunset MDW_IB_HOST      # falls back to 127.0.0.1See docker/ib-gateway/README.md for the full step-by-step provisioning guide, Tailscale ACL policy, client enrollment, 2FA reauth runbook, and volume backup procedures.
IBC provides:
- Auto login
- Session recovery
- Daily restarts
python scripts/install_ibc_secure_service.py~/ibc/bin/start-secure-ibc-service.sh
~/ibc/bin/stop-secure-ibc-service.sh
~/ibc/bin/status-secure-ibc-service.shThe IBC service is machine-level, not repo-scoped.
- 
IB Gateway running ( 127.0.0.1:4001by default)
- 
Configurable via: - CLI flags (--host,--port)
- Env vars (MDW_IB_HOST,MDW_IB_PORT)
 
- CLI flags (
source ~/market-warehouse/.venv/bin/activate
# Default (Mag 7)
python scripts/fetch_ib_historical.py
# Specific tickers
python scripts/fetch_ib_historical.py --tickers AAPL NVDA
# Preset universe
python scripts/fetch_ib_historical.py --preset presets/sp500.json
# Futures
python scripts/fetch_ib_historical.py --preset presets/futures-index.json --asset-class futures
# Volatility (CBOE direct)
python scripts/fetch_cboe_volatility.pypython scripts/fetch_ib_historical.py --preset presets/sp500.json --backfill- Fills only missing history
- Preserves existing data
- Independent cursor tracking
python scripts/daily_update.pyCommon flags:
--dry-run
--force
--target-date YYYY-MM-DD
--preset presets/sp500.json
--asset-class {equity|volatility|futures}- Detects missing trading days
- Fetches only gaps
- Validates OHLCV
- Atomic snapshot updates
- Fallback recovery if IB fails
python scripts/rebuild_duckdb_from_parquet.pylaunchctl load ~/Library/LaunchAgents/com.market-warehouse.daily-update.plist- Daily sync: 13:05 PT (4:05 PM ET)
- Watchdog: 18:30 PT
- Automatic retries
- Email alerts on failure
- Optional AI-generated summaries
npm installExample config:
MDW_ALERT_EMAIL_TO="you@example.com"
MDW_ALERT_SMTP_URL="smtp://user:pass@mail.example.com:587"python -m pytest tests/ -vpython -m pytest tests/ -v --cov=clients --cov=scripts- ✅ 100% coverage enforced
ln -sf ../../scripts/pre-commit-secrets-scan.sh .git/hooks/pre-commitDetects:
- API keys
- credentials
- private keys
- .envleaks
All volume is split-adjusted to ensure consistency across time.
Used for:
- Benchmarking
- Concurrency
- Production simulation
~/market-warehouse/scripts/start_clickhouse.sh
~/market-warehouse/scripts/init_clickhouse.sh
~/market-warehouse/scripts/stop_clickhouse.shpython scripts/write_sample_parquet.py- 
Store all data in Parquet (bronze) 
- 
Use DuckDB for: - research
- backtesting
 
- 
Use ClickHouse for: - large-scale queries
- production-like workloads
 
- Use inline PRIMARY KEY
- Avoid reserved keywords (right→option_right)
clickhouse-client --query "SELECT version()"./setup_market_warehouse.sh \
  --start-clickhouse \
  --init-clickhouse \
  --with-sample-data \
  --smoke-testMIT